TO DO
- install Atom package for icons in tabs ✅
- Git command for pushing and pulling
    -git commit -a -m "Further JS progress"
- Make #menu transparent or .filter(blur) when < 650px ✅
- Selection mechanism when < 650
- Make Safari-safe failsafe for wavy lines ✅
- Menu list items should be hyperlinks 

While I'm here:

Get to next module on SQL Code Academy
Google before after blur
Fetch API data
